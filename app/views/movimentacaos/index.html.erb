<div class='container'>
  <h1>Sistema de estoque</h1>

  <h2>Inserir movimentações</h2>

  <%= form_with url: create_movimentacao_path do |form| %>
    <%= form.file_field :movimentacao_csv, accept: '.csv', required: true %>
    <%= form.submit 'Cadastrar movimentações' %>
  <% end %>

  <h2>Locais de armazenamento</h2>

  <div class='stock-container'>
    <% @storages.each do |armazenamento| %>
      <div class='storage-container'>
        <h2><%= armazenamento.nome %></h2>

        <table>
          <thead>
            <tr>
              <th>Produto</th>
              <th>Quantidade</th>
            </tr>
          </thead>
          <tbody>
            <% products_in_stock(armazenamento).each do |product| %>
              <tr>
                <td><%= product[:nome] %></td>
                <td><%= product[:quantidade] %></td>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>
    <% end %>
  </div>
</div>
